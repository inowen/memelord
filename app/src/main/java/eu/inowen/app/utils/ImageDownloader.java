package eu.inowen.app.utils;


/**
 * - Take a url name through the constructor.
 *   - Set a path for download, giving the image some unique name f(url)
 *   - Add the extension (jpg, png, ...)
 *
 * - Download method: Downloads the image and returns the path where the image was stored.
 */
public class ImageDownloader {

    private String urlStr;
    private String destination;

    /**
     * 
     * @param url
     */
    public ImageDownloader(String url) {

    }

    public String download() {

    }
}
